name: CI
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
      - uses: sdkman/sdkman-action@v1
      - run: ./gradlew clean build
